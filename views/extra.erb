@purchase1 = Purchase.find(params.fetch("id"))
params.fetch("selected_product").each do |product_ids|
product = Product.find(product_ids.to_i)
@total += product.price
end
@product1 = Product.find(params.fetch("selected_product"))
@purchase1.products << product1
@total = purchase1.total


<% @product_ids.each do |product_id| %>
<%= Product.find(product_id).product_name %>

<% end %>
